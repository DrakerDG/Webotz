#ifndef STATE_HPP
#define STATE_HPP

enum class CheckState {     // Check point status class
  WAITING,
  RUNNING
};

enum class ModeState {     // Check point status class
  FIRST,
  SECOND
};

#endif
